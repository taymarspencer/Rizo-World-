# Rizo.game v86 — Launch Hotfix

- Fixes the v85 white-screen release failure path.
- ZIP is intentionally FLAT: index.html is at the archive root.
- Boot shell now stays visible until a real playable surface survives two paint turns.
- If startup fails, recovery UI remains visible instead of allowing a blank screen.
- Runtime/page/service-worker build marker: v86-launch-hotfix.
