# Rizo Defense v68 — Deleted / Superseded Economy Systems

- Per-world defense reward multipliers used as general economy inflation.
- Runtime duplicate of the deployment escalation formula.
- Golden active payday formula based on `currentWave` (`45 + currentWave*6 + upgrade*18` style growth).
- Unbounded linear Golden duplicate stacking behavior.
- Simulation/history-dependent five-second placement refund logic.
- `combatStarted` as an economy-permission proxy.
- Direct trust in checkpoint `spent` for tower sell investment.
- Unsigned one-shot world-perk state in the current checkpoint format.
- Map reward arguments in permanent Ember and wave-clear calculation paths.

No large CSS override block was added in Phase 5.
